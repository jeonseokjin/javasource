package access;

/*
 * 접근 제한자
 * -객체 생성을 막아야 하는 경우
 * -특정 메소드 호출 못하도록 하는 경우
 * -객체의 속성을 외부에서 함부로 변경하지 못하도록 하는 경우
 * -종류 : private < default(package) < protected < public(이 순서로 허용 범위가 넓어짐)
 * 		: default : 접근제한자를 안 붙이면 됨
 * -클래스의 접근 제한
 * -public(같은 패키지, 다른 패키지 가능), default(같은 패키지)
 * 
 * -멤버변수의 접근 제한(접근제한자 모두 사용가능)
 * -private : 같은 패키지건 다른 패키지건 상관없이 멤버변수 사용불가
 */

public class Card {

}
